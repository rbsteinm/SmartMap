<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/notification_activity"
    android:layout_width="@dimen/notification_activity_width"
    android:layout_height="@dimen/notification_activity_height"
    android:layout_gravity="bottom"
    tools:context="ch.epfl.smartmap.activities.NotificationsActivity" >
    
    <ListView
        android:id="@android:id/list"
        android:tag="@+id/notifications_list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginLeft="14dp"/>

</RelativeLayout>